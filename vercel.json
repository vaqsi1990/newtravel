{
    "buildCommand": "cd travel && npm install && npm run build",
    "outputDirectory": "travel/dist",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/backend/(.*)",
        "destination": "/backend/api.js"
      },
      {"source": "/(.*)", "destination": "/"}
    ]
  }


  
